
div(data-role='page')
  div(data-role='header')
    h1 b@mobile

  div(data-role='content')
    ul(data-role='listview', data-inset='true')
      - each post in data
        li
          a(href='#')
            h3 #{post.postText}
            p #{post.postTotalAgrees} agrees / #{post.postTotalDisagrees} disagrees / #{post.postTotalNewsworthies} newsworthies
            p.prettydate(title='#{post.postCreated.replace(' ','T')}')
            p
              span.ui-li-count #{post.postTotalReplies}
